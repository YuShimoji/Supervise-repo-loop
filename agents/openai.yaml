interface:
  display_name: "シンプル・コアループ互換入口"
  short_description: "Bootstrap the canonical Coordinator plus FrontierBoard loop"
  default_prompt: "Use $supervise-repo-loop to bootstrap or enter the simplified development loop."

policy:
  allow_implicit_invocation: false
