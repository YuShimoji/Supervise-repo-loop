# supervise-repo-loop backlog

This is the durable queue for incomplete work that must survive task handoff.
It does not grant live-state authority. Static implementation items are owned
here; portfolio execution items remain owned by the one bound primary
Coordinator and require the deterministic Mission value gate before dispatch.

## Active implementation queue

| ID | Purpose | Effect | Requirements | State | Owner | Next move |
|---|---|---|---|---|---|---|
| B-001 | Enforce one live-state writer | Prevent repair, audit, and reporting tasks from becoming a second Coordinator | Exact `CODEX_THREAD_ID` binding, canonical installed state paths, idle-only explicit rebind, negative tests | completed | supervise-repo-loop source | Installed-runtime proof is tracked by B-006 |
| B-002 | Gate legacy Mission dispatch | Prevent an old `WORK_ORDER_RECEIVED` or early pending Mission without a value contract from reaching a Worker | Scheduler-level admission action, corrected-contract route, replay-safe contract attachment | completed | supervise-repo-loop source | Installed-runtime proof is tracked by B-006 |
| B-003 | Align Prompt, schema, and runtime value fields | Keep quick-win decisions deterministic instead of prose-only | Exact `value_contract_v1` field list and admission behavior in the machine-readable Prompt and schemas | completed | supervise-repo-loop source | Installed-runtime proof is tracked by B-006 |
| B-004 | Cover live mutation entry points | Prevent migration, compile, render, event, runtime, and route commands from bypassing the writer fence | Secondary-task negative tests and canonical-path assertions | completed | supervise-repo-loop source | Installed-runtime proof is tracked by B-006 |
| B-005 | Make user action and roadmap status complete | Show whether the user must act, all instructions in one place, and each block's position in the project path | One top-level user card, per-project roadmap, deterministic renderer and validation | completed | supervise-repo-loop source | Primary must regenerate the live projection after B-006 |
| B-006 | Deploy and prove the corrected runtime | Move static changes to the installed skill without touching live state from this repair task | Green source tests, diff check, idle primary, allowlisted sync, installed tests, secondary live rejection check | completed | supervise-repo-loop source | Primary must reread the installed contract and regenerate its live portfolio |
| B-011 | Make the development and Codex scheduling structure portable | Let another computer reproduce the static runtime without cloning writer identity or host-local automation state | Portable manifest, exact remote/upstream check, static install parity check, ignored local tool state, cross-host runbook | completed | supervise-repo-loop source | Continue with the genuine receiving-host canary in B-012 |
| B-012 | Prove a second-host bootstrap | Confirm the documented boundary on a genuinely separate Codex installation | Fresh clone, new host-local installed skill, new exact primary binding, paused recovery heartbeat, no copied `state/` or task ID | waiting_external | User / receiving-host operator | Run the host checklist on the receiving computer; do not move an in-flight route as part of this canary |
| B-013 | Deploy the frontier reconciliation gate | Prevent stale artifact re-promotion in the installed runtime | Green source review, static allowlist sync without `state/`, installed tests, primary-owned read-only frontier audit, explicit legacy reconciliation | installed_pending_primary_reconciliation | supervise-repo-loop source / exact primary Coordinator | Review and merge the isolated source change; the exact primary then applies explicit live frontier reconciliation without inference |
| B-014 | Deploy the generic project-context frontier | Prevent Supervisor tunnel vision and user-visible current-position drift across every existing and future project | Context ledger/envelope schemas, double CAS, portfolio v4, four existing plus web/game/media canaries, full regression, static-only install parity | installed_pending_primary_reconciliation | supervise-repo-loop source / exact primary Coordinator | Review and merge the isolated source change; the exact primary then applies explicit context reconciliation events without inferred live state |
| B-015 | Preserve WAITING_USER through project-context migration | Let the canonical renderer expose an exact user card while ordinary work remains context-gated | Synthetic revision-194-shaped regression, full source/installed tests, static parity, primary-owned live regeneration | completed | supervise-repo-loop source / exact primary Coordinator | The exact primary rereads the installed runtime and regenerates the canonical portfolio from scheduler revision 194 |
| B-016 | Keep unresolved reconciliation claimable | Prevent local abstention receipts, one user card, an out-of-scope same-priority row, or a false Mission requirement from stopping selected project routes | Exact Supervisor-bound frontier/context actions, Missionless typed frontier reducer, typed context reducer, highest-priority ready-set claim, revision-194 live restart, full source/installed tests | installed_pending_result_retry | supervise-repo-loop source / exact primary Coordinator | Retry the same ClipPipeGen typed result without changing its route identity, then require the frontier ledger to advance |

## Primary Coordinator execution queue

These items are not authorization to start work. After B-006, the bound
primary Coordinator must re-read the installed contract and admit only an
exact `value_contract_v1` quick win. `NO_WORK` or parking is valid.

| ID | Purpose | Effect | Requirements | State | Owner | Next move |
|---|---|---|---|---|---|---|
| B-007 | Reconcile ClipPipeGen to its certified current gate | Remove chronology drift without selecting an artifact from this static backlog | Current Git/authority high water, exact Supervisor/human disposition lineage, FrontierCertificate, ProjectContextRecord | context_and_frontier_reconciliation_required | exact primary Coordinator after B-013/B-014 | Run both read-only audits; do not present any historical Clip artifact for review until reconciliation applies |
| B-008 | Reconcile FastFictionFactory to its certified current gate | Prevent benchmark/Densou or another historical candidate from replacing the actual current disposition | Current Git/authority high water, exact Supervisor result identity, epoch/revision double CAS, ProjectContextRecord | context_and_frontier_reconciliation_required | exact primary Coordinator after B-013/B-014 | Audit and apply only exact current frontier and context events; delivery or an old static row is insufficient |
| B-009 | Reconcile NLMYTGen to its certified current gate | Keep runtime/evidence work bound to the actual accepted frontier and whole-project bottleneck | Current Git/authority high water, exact runtime/Supervisor lineage, FrontierCertificate, ProjectContextRecord | context_and_frontier_reconciliation_required | exact primary Coordinator after B-013/B-014 | Audit without probing or dispatch; reconcile all active lanes and the exact current disposition before ordinary work |
| B-010 | Reconcile Residual Atlas to its certified current gate | Prevent an older preview or narrowed lane view from outranking later human acceptance | Current Git/authority high water, human/Supervisor lineage, FrontierCertificate, ProjectContextRecord | context_and_frontier_reconciliation_required | exact primary Coordinator after B-013/B-014 | Audit and preserve human precedence across every active lane; do not request a generic successor from legacy state |
